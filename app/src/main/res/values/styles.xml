<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextViewDefault">
        <item name="fontPath">@string/RobotoRegular</item>
    </style>

    <style name="RobotoBlack">
        <item name="fontPath">@string/RobotoBlack</item>
    </style>

    <style name="TextViewLight">
        <item name="fontPath">@string/RobotoLight</item>
    </style>

    <style name="TextViewThin">
        <item name="fontPath">@string/RobotoThin</item>
    </style>

    <style name="ErrorToast">
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@string/RobotoThin</item>
        <item name="android:colorBackground">@color/green_1</item>
        <item name="android:icon">@drawable/ic_error</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
